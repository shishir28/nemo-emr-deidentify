venv/
__pycache__/
*.pyc
.env
.env.*

# Data — mount as volumes instead
data/

# Checkpoints — mount as volumes instead
models/checkpoints/
*.pt
*.pth
*.nemo
*.ckpt

# Training scripts — not needed in serving container
scripts/

# Tests — not needed in serving container
tests/
.pytest_cache/

# Audit logs — mount as volume at runtime
logs/

# Dev files
notebooks/
.git/
.gitignore
*.log
docs/
